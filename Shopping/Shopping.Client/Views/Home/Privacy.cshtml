@model Shopping.Client.Models.coindesk
@{
    ViewData["Title"] = "Privacy Policy";
}

<h1>@ViewData["Title"]</h1>


<p>Use this page to detail your site's privacy policy.</p>

@*@foreach (var item in Model) {
    <tr>
         <td>
            @Html.DisplayFor(modelItem => item)
        </td>
    </tr>
}*@

<p>Model.bpi.USD.description</p>
<p>@Html.DisplayFor(modelItem => Model.bpi)</p>
<h1>@Html.DisplayFor(modelItem => Model.bpi.USD.description)</h1>
